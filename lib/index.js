"use strict";

/**
 * execLimiterCli
 * Run multiple shell commands in parallel, but with a limit.
 *
 * @name execLimiterCli
 * @function
 * @param {Number} a Param descrpition.
 * @param {Number} b Param descrpition.
 * @return {Number} Return description.
 */
module.exports = function execLimiterCli (a, b) {
    return a + b;
};